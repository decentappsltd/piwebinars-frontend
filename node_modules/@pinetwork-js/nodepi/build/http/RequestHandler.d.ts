import { OptionsOfUnknownResponseBody } from 'got';
import { HttpManager } from './HttpManager';
/**
 * The http request handler.
 */
export declare class RequestHandler {
    private readonly manager;
    /**
     * The request queue.
     */
    private readonly queue;
    constructor(manager: HttpManager);
    /**
     * Push the request to the queue waiting for its execution.
     *
     * @param gotOptions - The got options for the request.
     * @returns The payload of the request if there is one.
     */
    push(gotOptions: OptionsOfUnknownResponseBody): Promise<Record<PropertyKey, unknown> | undefined>;
    /**
     * Execute the request.
     *
     * @param gotOptions - The got options for the request.
     * @param retries - The number of retries performed.
     * @returns The payload of the request if there is one.
     */
    private execute;
}
