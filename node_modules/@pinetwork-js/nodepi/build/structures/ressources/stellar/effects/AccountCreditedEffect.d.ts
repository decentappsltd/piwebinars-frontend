import { AccountCredited, EffectType } from 'stellar-sdk/lib/types/effects';
import { Client } from '../../../../clients';
import { Effect } from './Effect';
import { EffectTypeNames } from '.';
/**
 * Structure representing a Stellar Account Credited Effect.
 */
export declare class AccountCreditedEffect extends Effect<EffectTypeNames.accountCredited, EffectType.account_credited> {
    /**
     * The amount of Pi credited to the account.
     */
    amount: number;
    constructor(client: Client, data: AccountCredited);
    $patch(data: AccountCredited): void;
}
