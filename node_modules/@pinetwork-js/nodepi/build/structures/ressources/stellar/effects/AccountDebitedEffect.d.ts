import { AccountDebited, EffectType } from 'stellar-sdk/lib/types/effects';
import { Client } from '../../../../clients';
import { Effect } from './Effect';
import { EffectTypeNames } from '.';
/**
 * Structure representing a Stellar Account Debited Effect.
 */
export declare class AccountDebitedEffect extends Effect<EffectTypeNames.accountDebited, EffectType.account_debited> {
    /**
     * The amount of Pi debited from the account.
     */
    amount: number;
    constructor(client: Client, data: AccountDebited);
    $patch(data: AccountDebited): void;
}
