import { AccountHomeDomainUpdated, EffectType } from 'stellar-sdk/lib/types/effects';
import { Client } from '../../../../clients';
import { Effect } from './Effect';
import { EffectTypeNames } from '.';
/**
 * Structure representing a Stellar Account Home Domain Updated Effect.
 */
export declare class AccountHomeDomainUpdatedEffect extends Effect<EffectTypeNames.accountHomeDomainUpdated, EffectType.account_home_domain_updated> {
    /**
     * The updated home domain of the account.
     */
    homeDomain: string;
    constructor(client: Client, data: AccountHomeDomainUpdated);
    $patch(data: AccountHomeDomainUpdated): void;
}
