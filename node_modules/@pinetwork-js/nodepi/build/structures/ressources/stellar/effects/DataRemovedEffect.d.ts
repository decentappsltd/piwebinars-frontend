import { DataRemoved, EffectType } from 'stellar-sdk/lib/types/effects';
import { Client } from '../../../../clients';
import { Effect } from './Effect';
import { EffectTypeNames } from '.';
/**
 * Structure representing a Stellar Data Removed Effect.
 */
export declare class DataRemovedEffect extends Effect<EffectTypeNames.dataRemoved, EffectType.data_removed> {
    /**
     * The name of the removed account data.
     */
    name: string;
    constructor(client: Client, data: DataRemoved);
    $patch(data: DataRemoved): void;
}
