import { EffectType, SequenceBumped } from 'stellar-sdk/lib/types/effects';
import { Client } from '../../../../clients';
import { Effect } from './Effect';
import { EffectTypeNames } from '.';
/**
 * Structure representing a Stellar Sequence Bumped Effect.
 */
export declare class SequenceBumpedEffect extends Effect<EffectTypeNames.sequenceBumped, EffectType.sequence_bumped> {
    /**
     * The new sequence.
     */
    newSequence: string;
    constructor(client: Client, data: SequenceBumped);
    $patch(data: SequenceBumped): void;
}
