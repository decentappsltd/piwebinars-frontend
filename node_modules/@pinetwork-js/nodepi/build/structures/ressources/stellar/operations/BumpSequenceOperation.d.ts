import { Horizon, ServerApi } from 'stellar-sdk';
import { Client } from '../../../../clients';
import { Operation } from './Operation';
/**
 * Structure representing a Stellar Bump Sequence Operation.
 */
export declare class BumpSequenceOperation extends Operation<Horizon.OperationResponseType.bumpSequence, Horizon.OperationResponseTypeI.bumpSequence> {
    /**
     * The new desired value for the source account’s sequence number.
     */
    bumpTo: string;
    constructor(client: Client, data: ServerApi.BumpSequenceOperationRecord);
    $patch(data: ServerApi.BumpSequenceOperationRecord): void;
}
