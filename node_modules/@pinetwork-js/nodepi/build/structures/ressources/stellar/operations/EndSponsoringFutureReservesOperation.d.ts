import { Horizon, ServerApi } from 'stellar-sdk';
import { Client } from '../../../../clients';
import { Account } from '../Account';
import { Operation } from './Operation';
/**
 * Structure representing a Stellar End Sponsoring Future Reserves Operation.
 */
export declare class EndSponsoringFutureReservesOperation extends Operation<Horizon.OperationResponseType.endSponsoringFutureReserves, Horizon.OperationResponseTypeI.endSponsoringFutureReserves> {
    /**
     * The id of the account which initiated the sponsorship.
     */
    sponsorId: string;
    constructor(client: Client, data: ServerApi.EndSponsoringFutureReservesOperationRecord);
    $patch(data: ServerApi.EndSponsoringFutureReservesOperationRecord): void;
    /**
     * Get the account which initiated the sponsorship.
     *
     * @param forceUpdate - If set to `true`, it will not check in the cache and directly make a request to retrieve the sponsor account instead.
     * @returns The account of the sponsor.
     */
    getSponsorAccount(forceUpdate?: boolean): Promise<Account>;
}
