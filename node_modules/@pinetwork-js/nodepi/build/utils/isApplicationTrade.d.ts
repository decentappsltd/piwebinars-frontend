import { ServerApi } from 'stellar-sdk';
import { StellarClient } from '../clients';
export declare function isApplicationTrade(this: StellarClient, trade: ServerApi.TradeRecord, applicationPublicKey: string): Promise<boolean>;
