function rewardPi() {
  if date.getDate() = Date(x,x,1) {
  document.getElementById(clicked_id) {
    Pi.createPayment(amount: 'x*0.8*'${view_count}'', memo: 'PI REWARD FROM YOUR WEBINARS', metadata: { 0000 }, address: '${username}')
  };
}

x = //one GBP worth of Pi
